# 📘 Moment Snap 프로젝트 완벽 가이드

> **모든 것을 하나로!** 프로젝트 설정부터 배포, 개념 이해까지 완벽 정리  
> **작성일**: 2025년 11월 3일  
> **버전**: 2.0.0

---

## 🎯 이 문서의 목적

기존에 흩어진 20개 이상의 MD 파일을 **하나로 통합**했습니다!  
이제 이 문서 하나만 읽으면 프로젝트의 모든 것을 이해할 수 있습니다.

---

## 📚 목차

### 🚀 **Part 1: 빠른 시작 (5분)**
1. [프로젝트 소개](#1-프로젝트-소개)
2. [5분 안에 시작하기](#2-5분-안에-시작하기)
3. [체크리스트](#3-체크리스트)

### 🎓 **Part 2: 핵심 개념 이해**
4. [기술 스택 완벽 이해](#4-기술-스택-완벽-이해)
5. [프로젝트 구조 분석](#5-프로젝트-구조-분석)
6. [데이터베이스 스키마](#6-데이터베이스-스키마)

### 🛠️ **Part 3: 상세 설정**
7. [Supabase 설정](#7-supabase-설정)
8. [Gmail 이메일 알림](#8-gmail-이메일-알림)
9. [포트폴리오 이미지 관리](#9-포트폴리오-이미지-관리)
10. [관리자 보안 강화](#10-관리자-보안-강화)

### 🎨 **Part 4: 커스터마이징**
11. [비즈니스 정보 변경](#11-비즈니스-정보-변경)
12. [색상 테마 변경](#12-색상-테마-변경)
13. [다국어 관리](#13-다국어-관리)

### 🚀 **Part 5: 배포 및 운영**
14. [Vercel 배포](#14-vercel-배포)
15. [도메인 연결](#15-도메인-연결)
16. [비용 안내](#16-비용-안내)
17. [사장님께 인수인계](#17-사장님께-인수인계)

### 🆘 **Part 6: 문제 해결**
18. [자주 묻는 질문 (FAQ)](#18-자주-묻는-질문-faq)
19. [트러블슈팅](#19-트러블슈팅)

---

# Part 1: 빠른 시작

## 1. 프로젝트 소개

### 📸 Moment Snap이란?

스냅사진 전문 포토그래퍼를 위한 **완전 무료** 웹사이트입니다!

**완성된 기능:**
- ✅ 반응형 디자인 (모바일/태블릿/PC)
- ✅ 포트폴리오 갤러리 (카테고리 필터)
- ✅ 서비스 패키지 소개
- ✅ 문의 접수 시스템
- ✅ 관리자 대시보드
- ✅ 이메일 알림 (Gmail 무료)
- ✅ 3개 국어 지원 (한국어/영어/일본어)

**월 비용:**
- 호스팅: **무료** (Vercel)
- 데이터베이스: **무료** (Supabase 500MB)
- 이메일: **무료** (Gmail API, 하루 500통)
- 도메인: **연 12,000원** (선택사항)

---

## 2. 5분 안에 시작하기

### Step 1: 패키지 설치 (1분)

```bash
# 프로젝트 폴더로 이동
cd photo-studio

# 패키지 설치
npm install
```

### Step 2: 환경 변수 설정 (2분)

1. `env.example` 파일을 복사하여 `.env.local` 생성:
```bash
# Windows (PowerShell)
Copy-Item env.example .env.local

# Mac/Linux
cp env.example .env.local
```

2. `.env.local` 파일 열어서 수정:
```bash
# Supabase (필수)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Gmail (선택사항 - 이메일 알림)
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-digit-password
```

### Step 3: 개발 서버 실행 (1분)

```bash
npm run dev
```

브라우저에서 http://localhost:3000 접속!

### Step 4: Supabase 설정 (2분)

1. https://supabase.com 가입
2. 새 프로젝트 생성
3. SQL Editor에서 `supabase-setup.sql` 실행
4. Settings > API에서 URL과 Key 복사하여 `.env.local`에 붙여넣기

**완료! 🎉**

---

## 3. 체크리스트

### ✅ 개발 환경 설정
- [ ] Node.js 설치됨 (v18 이상)
- [ ] `npm install` 완료
- [ ] `.env.local` 파일 생성
- [ ] `npm run dev` 실행 성공

### ✅ Supabase 설정
- [ ] Supabase 계정 생성
- [ ] 프로젝트 생성
- [ ] `supabase-setup.sql` 실행
- [ ] API Keys 복사 완료

### ✅ 기능 테스트
- [ ] 홈페이지 로딩 확인
- [ ] 포트폴리오 페이지 확인
- [ ] 문의 폼 제출 테스트
- [ ] 관리자 로그인 테스트

---

# Part 2: 핵심 개념 이해

## 4. 기술 스택 완벽 이해

### 🎓 Next.js 16.0.1이란?

**React의 프레임워크** - React를 더 쉽고 강력하게 만들어줍니다.

#### React vs Next.js

| 항목 | React (CRA) | Next.js |
|------|-------------|---------|
| **렌더링** | 클라이언트만 | 서버 + 클라이언트 |
| **SEO** | ❌ 약함 | ✅ 강력 |
| **라우팅** | react-router 필요 | 파일 기반 (자동) |
| **API** | 별도 서버 필요 | 내장 API Routes |
| **이미지 최적화** | 수동 | 자동 |
| **초기 로딩** | 느림 | 빠름 |

#### 파일 기반 라우팅

```
src/app/
├── page.tsx              → /           (홈)
├── portfolio/
│   └── page.tsx          → /portfolio  (포트폴리오)
├── services/
│   └── page.tsx          → /services   (서비스)
└── contact/
    └── page.tsx          → /contact    (문의)
```

**폴더만 만들면 자동으로 URL이 생성됩니다!**

---

### ⚡ Turbopack이란?

**Webpack의 후속작** - Next.js 16에서 기본으로 사용하는 **초고속 번들러**

#### Webpack vs Turbopack

| 항목 | Webpack | Turbopack |
|------|---------|-----------|
| **속도** | 느림 | **700배 빠름** |
| **언어** | JavaScript | Rust (저수준 언어) |
| **개발 서버 시작** | 10초 | 1초 |
| **HMR (Hot Reload)** | 2초 | 0.1초 |

**왜 빠른가?**
- Rust로 작성 (메모리 효율 극대화)
- 병렬 처리 최적화
- 증분 컴파일 (변경된 파일만 다시 빌드)

**사용법:**
```bash
# Next.js 16에서는 자동으로 Turbopack 사용!
npm run dev
```

---

### 🎨 Tailwind CSS v4란?

**유틸리티 우선 CSS 프레임워크** - HTML을 떠나지 않고 스타일링

#### 기존 CSS vs Tailwind

**기존 방식:**
```css
/* styles.css */
.button {
  background-color: #3b82f6;
  color: white;
  padding: 0.5rem 1rem;
  border-radius: 0.25rem;
}
```

```html
<button class="button">클릭</button>
```

**Tailwind 방식:**
```html
<button class="bg-blue-500 text-white px-4 py-2 rounded">
  클릭
</button>
```

**장점:**
- ✅ CSS 파일 불필요
- ✅ 클래스명 고민 없음
- ✅ 반응형 쉬움: `md:text-lg lg:text-xl`
- ✅ 다크모드 쉬움: `dark:bg-gray-800`

**v4의 새 기능:**
```css
/* globals.css */
@import "tailwindcss";

@theme {
  --color-primary: #78716c;
  --color-accent: #d4a574;
}
```

```html
<div class="bg-primary text-accent">커스텀 색상!</div>
```

---

### 🗄️ Supabase란?

**Firebase의 오픈소스 대안** - PostgreSQL 기반의 백엔드 서비스

#### Firebase vs Supabase

| 항목 | Firebase | Supabase |
|------|----------|----------|
| **DB** | NoSQL (Firestore) | SQL (PostgreSQL) |
| **가격** | 비쌈 | 무료 500MB |
| **쿼리** | 제한적 | SQL 모든 기능 |
| **벤더 락인** | 심함 | 약함 (오픈소스) |
| **실시간** | ✅ | ✅ |
| **인증** | ✅ | ✅ |
| **Storage** | ✅ | ✅ |

**핵심 기능:**

1. **Database (PostgreSQL)**
```typescript
// 데이터 조회
const { data } = await supabase
  .from('contacts')
  .select('*')
  .order('created_at', { ascending: false });
```

2. **Authentication (인증)**
```typescript
// 로그인
const { data, error } = await supabase.auth.signInWithPassword({
  email: 'admin@example.com',
  password: 'password',
});
```

3. **Storage (파일 저장)**
```typescript
// 파일 업로드
const { data, error } = await supabase.storage
  .from('portfolio-images')
  .upload('photo.jpg', file);
```

4. **Row Level Security (RLS)**
```sql
-- 모든 사람이 조회 가능
CREATE POLICY "Public read" ON portfolios
  FOR SELECT USING (true);

-- 관리자만 삭제 가능
CREATE POLICY "Admin delete" ON portfolios
  FOR DELETE USING (
    (auth.jwt()->'user_metadata'->>'role') = 'admin'
  );
```

---

### 📧 Unsplash란?

**무료 고품질 이미지 제공 사이트** - 상업적 사용 가능!

#### 왜 Unsplash를 사용하나?

**개발 단계:**
- ✅ 완전 무료
- ✅ 고품질 이미지
- ✅ CDN 제공 (빠른 로딩)
- ✅ 저작권 걱정 없음
- ✅ 테스트용으로 완벽

**현재 프로젝트:**
```typescript
// INSERT_SAMPLE_PORTFOLIOS.sql
const imageUrl = 'https://images.unsplash.com/photo-1519741497674-611481863552?w=1200&q=80';
```

#### Unsplash vs 실제 사진

| 항목 | Unsplash | 실제 사진 (Supabase) |
|------|----------|----------------------|
| **비용** | 무료 | 무료 (500MB까지) |
| **속도** | 빠름 (CDN) | 빠름 |
| **소유권** | 없음 | ✅ 있음 |
| **관리** | 불가능 | ✅ 가능 (관리자 페이지) |
| **변경 위험** | URL 변경 가능 | 영구 보존 |

**언제 교체하나?**
- 실제 촬영 사진이 준비되면
- Supabase Storage로 업로드
- 관리자 페이지에서 쉽게 관리

**Unsplash 사용법:**
```bash
# next.config.ts에 도메인 추가 필수!
images: {
  remotePatterns: [
    {
      protocol: 'https',
      hostname: 'images.unsplash.com',
    },
  ],
}
```

---

### 🌍 i18n (다국어)란?

**Internationalization** - 국제화 (한국어/영어/일본어)

#### 동작 원리

```
src/locales/
├── ko/
│   └── common.json     → 한국어
├── en/
│   └── common.json     → 영어
└── ja/
    └── common.json     → 일본어
```

**사용 예시:**
```typescript
// src/locales/ko/common.json
{
  "nav": {
    "home": "홈",
    "portfolio": "포트폴리오"
  }
}

// src/locales/en/common.json
{
  "nav": {
    "home": "Home",
    "portfolio": "Portfolio"
  }
}
```

```tsx
// 컴포넌트에서 사용
const t = useTranslation();

<Link href="/">{t('nav.home')}</Link>
// 한국어: 홈
// 영어: Home
```

---

## 5. 프로젝트 구조 분석

### 📁 전체 구조

```
photo-studio/
├── src/                          # 소스 코드
│   ├── app/                      # Next.js App Router (페이지)
│   │   ├── page.tsx             # 홈 (/)
│   │   ├── layout.tsx           # 전역 레이아웃
│   │   ├── globals.css          # 전역 스타일
│   │   ├── portfolio/
│   │   │   └── page.tsx         # 포트폴리오 페이지
│   │   ├── services/
│   │   │   └── page.tsx         # 서비스 페이지
│   │   ├── contact/
│   │   │   └── page.tsx         # 문의 페이지
│   │   ├── auth/
│   │   │   └── page.tsx         # 로그인 페이지
│   │   ├── admin/
│   │   │   ├── page.tsx         # 관리자 대시보드
│   │   │   └── portfolio/
│   │   │       └── page.tsx     # 포트폴리오 관리
│   │   └── api/
│   │       └── send-email/
│   │           └── route.ts     # 이메일 발송 API
│   ├── components/               # 재사용 컴포넌트
│   │   ├── Navbar.tsx           # 네비게이션
│   │   ├── Footer.tsx           # 푸터
│   │   └── LanguageSwitcher.tsx # 언어 전환
│   ├── config/
│   │   └── business-info.ts     # 📞 비즈니스 정보 (제일 중요!)
│   ├── contexts/
│   │   └── LanguageContext.tsx  # 다국어 Context
│   ├── hooks/
│   │   └── useTranslation.ts    # 번역 Hook
│   ├── lib/
│   │   ├── supabase.ts          # Supabase 클라이언트
│   │   ├── admin-auth.ts        # 관리자 인증
│   │   ├── rate-limiter.ts      # 로그인 횟수 제한
│   │   └── session-manager.ts   # 세션 관리
│   └── locales/                  # 다국어 파일
│       ├── ko/common.json       # 한국어
│       ├── en/common.json       # 영어
│       └── ja/common.json       # 일본어
├── public/                       # 정적 파일
├── scripts/                      # 유틸리티 스크립트
│   └── download-and-upload-images.js
├── .env.local                    # 환경 변수 (직접 생성)
├── env.example                   # 환경 변수 예시
├── next.config.ts                # Next.js 설정
├── tailwind.config.ts            # Tailwind 설정
├── package.json                  # 패키지 정보
└── 📘_프로젝트_완벽_가이드.md     # 이 문서!
```

---

### 🎯 핵심 파일 설명

#### 1. `src/config/business-info.ts` ⭐ **제일 중요!**

**모든 비즈니스 정보를 여기서 관리합니다!**

```typescript
export const businessInfo = {
  name: 'Moment Snap',
  email: 'contact@momentsnap.com',
  phone: '+82-10-1234-5678',
  address: '서울특별시 강남구 테헤란로 123',
  
  social: {
    instagram: 'https://instagram.com/momentsnap',
    facebook: 'https://facebook.com/momentsnap',
  },
  
  hours: {
    weekday: '10:00 - 19:00',
    weekend: '예약제',
  },
};
```

**수정하면 자동으로 모든 페이지에 반영됩니다!**

---

#### 2. `src/lib/supabase.ts`

**Supabase 클라이언트 설정**

```typescript
import { createClient } from '@supabase/supabase-js';

export const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
);
```

---

#### 3. `src/app/layout.tsx`

**전역 레이아웃** - 모든 페이지에 공통 적용

```tsx
export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body>
        <LanguageProvider>
          <Navbar />
          {children}  {/* 각 페이지 내용 */}
          <Footer />
        </LanguageProvider>
      </body>
    </html>
  );
}
```

---

## 6. 데이터베이스 스키마

### 📊 테이블 구조

#### 1. `contacts` (문의 내역)

```sql
CREATE TABLE contacts (
  id uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  name text NOT NULL,
  email text NOT NULL,
  phone text,
  message text NOT NULL,
  created_at timestamp with time zone DEFAULT now()
);
```

**컬럼 설명:**
- `id`: 고유 식별자 (자동 생성)
- `name`: 고객 이름
- `email`: 이메일 주소
- `phone`: 전화번호 (선택)
- `message`: 문의 내용
- `created_at`: 문의 날짜/시간

---

#### 2. `portfolios` (포트폴리오)

```sql
CREATE TABLE portfolios (
  id uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  title text NOT NULL,
  category text NOT NULL,
  description text,
  image_url text NOT NULL,
  created_at timestamp with time zone DEFAULT now()
);
```

**카테고리:**
- `wedding`: 웨딩 스냅
- `couple`: 커플 스냅
- `profile`: 프로필 촬영
- `family`: 가족 사진
- `product`: 제품 촬영

---

#### 3. Storage Bucket (파일 저장소)

```
portfolio-images/
└── public/
    ├── wedding-01.jpg
    ├── couple-01.jpg
    └── profile-01.jpg
```

---

### 🔒 보안 정책 (RLS)

**Row Level Security** - 행 단위 보안

```sql
-- 누구나 조회 가능
CREATE POLICY "Public read" ON portfolios
  FOR SELECT USING (true);

-- 관리자만 추가/수정/삭제
CREATE POLICY "Admin only" ON portfolios
  FOR ALL USING (
    (auth.jwt()->'user_metadata'->>'role') = 'admin'
  );
```

---

# Part 3: 상세 설정

## 7. Supabase 설정

### 🔧 Step 1: 프로젝트 생성 (2분)

1. https://supabase.com 접속
2. **New Project** 클릭
3. 정보 입력:
   - Name: `photo-studio`
   - Database Password: **복사해두세요!**
   - Region: **Northeast Asia (Seoul)**
4. **Create new project** 클릭
5. 약 2분 대기...

---

### 🔧 Step 2: API Keys 복사 (1분)

1. 프로젝트 대시보드
2. **Settings** (왼쪽 하단 톱니바퀴)
3. **API** 클릭
4. 2개 복사:
   - `Project URL` → `.env.local`의 `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public key` → `.env.local`의 `NEXT_PUBLIC_SUPABASE_ANON_KEY`

---

### 🔧 Step 3: 데이터베이스 생성 (1분)

1. 왼쪽 메뉴 > **SQL Editor**
2. **New query** 클릭
3. 프로젝트의 `supabase-setup.sql` 파일 열기
4. 전체 내용 복사
5. SQL Editor에 붙여넣기
6. **Run** 버튼 (또는 Ctrl+Enter)
7. "Success. No rows returned" 확인

**생성된 것:**
- ✅ `contacts` 테이블
- ✅ `portfolios` 테이블
- ✅ RLS 정책
- ✅ 인덱스

---

### 🔧 Step 4: 관리자 계정 생성 (2분)

1. 왼쪽 메뉴 > **Authentication** > **Users**
2. **Add user** > **Create new user** 클릭
3. 정보 입력:
   - Email: `admin@yoursite.com`
   - Password: `강력한비밀번호!@#`
   - **Auto Confirm User**: ✅ 체크 필수!
4. **Create user** 클릭

**관리자 권한 부여:**

SQL Editor로 돌아가서 실행:

```sql
UPDATE auth.users
SET raw_user_meta_data = raw_user_meta_data || '{"role": "admin"}'::jsonb
WHERE email = 'admin@yoursite.com';
```

---

### 🔧 Step 5: Storage Bucket 생성 (1분)

1. 왼쪽 메뉴 > **Storage**
2. **New bucket** 클릭
3. 정보 입력:
   - Name: `portfolio-images`
   - **Public bucket**: ✅ 체크 필수!
4. **Create bucket** 클릭

**Storage 정책 설정:**

SQL Editor에서 실행:

```sql
-- 누구나 이미지 조회 가능
CREATE POLICY "Public can view portfolio images"
ON storage.objects FOR SELECT
USING (bucket_id = 'portfolio-images');

-- 관리자만 업로드 가능
CREATE POLICY "Admin can upload portfolio images"
ON storage.objects FOR INSERT
WITH CHECK (
  bucket_id = 'portfolio-images' AND
  (auth.jwt()->'user_metadata'->>'role') = 'admin'
);

-- 관리자만 삭제 가능
CREATE POLICY "Admin can delete portfolio images"
ON storage.objects FOR DELETE
USING (
  bucket_id = 'portfolio-images' AND
  (auth.jwt()->'user_metadata'->>'role') = 'admin'
);
```

---

### ✅ Supabase 설정 완료!

**테스트:**
1. http://localhost:3000/auth 접속
2. 생성한 계정으로 로그인
3. http://localhost:3000/admin 접속
4. 문의 내역이 보이면 성공!

---

## 8. Gmail 이메일 알림

### 📧 왜 Gmail인가?

**완전 무료! 광고 없음!**

| 항목 | Gmail API | EmailJS | SendGrid |
|------|-----------|---------|----------|
| **무료 한도** | 500통/일 | 200통/월 | 100통/일 |
| **비용** | 무료 | 무료 | 유료 전환 필요 |
| **설정 난이도** | 쉬움 | 매우 쉬움 | 복잡함 |
| **안정성** | ✅ 매우 높음 | 보통 | 높음 |

---

### 🔧 Gmail 설정 (5분)

#### Step 1: 2단계 인증 활성화 (2분)

1. https://myaccount.google.com 접속
2. **보안** 클릭
3. **2단계 인증** 찾아서 클릭
4. 안내에 따라 활성화

---

#### Step 2: 앱 비밀번호 생성 (2분)

1. 다시 **보안** 페이지
2. 검색창에 "앱 비밀번호" 입력
3. **앱 비밀번호** 클릭
4. 앱 이름 입력: `Moment Snap`
5. **생성** 클릭
6. **16자리 비밀번호 복사** (예: `abcd efgh ijkl mnop`)

---

#### Step 3: .env.local 수정 (1분)

```bash
# Gmail 설정 추가
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=abcdefghijklmnop  # 공백 제거한 16자리!
```

---

#### Step 4: 서버 재시작

```bash
# Ctrl+C로 서버 중지
# 다시 시작
npm run dev
```

---

### ✅ 테스트

1. http://localhost:3000/contact 접속
2. 문의 폼 작성
3. 제출
4. Gmail 확인 → 이메일 도착!

---

## 9. 포트폴리오 이미지 관리

### 🖼️ 두 가지 방법

#### 방법 1: Unsplash 사용 (현재 방식)

**장점:**
- ✅ 완전 무료
- ✅ 설정 불필요 (이미 완료)
- ✅ 테스트용으로 완벽

**단점:**
- ❌ 실제 작품이 아님
- ❌ URL 변경 가능성

**현재 상태:**
- `INSERT_SAMPLE_PORTFOLIOS.sql` 실행하면 12개 샘플 이미지 자동 삽입
- `next.config.ts`에 이미 설정 완료

```typescript
// next.config.ts
images: {
  remotePatterns: [
    {
      protocol: 'https',
      hostname: 'images.unsplash.com',  // Unsplash
    },
    {
      protocol: 'https',
      hostname: '*.supabase.co',  // Supabase Storage
    },
  ],
}
```

---

#### 방법 2: Supabase Storage (실제 운영)

**장점:**
- ✅ 직접 소유
- ✅ 영구 보존
- ✅ 관리자 페이지에서 쉽게 관리

**단점:**
- ⚠️ 초기 업로드 필요
- ⚠️ 용량 제한 (무료 1GB)

---

### 🚀 Supabase로 마이그레이션

#### Step 1: Storage Bucket 생성

**위의 [7. Supabase 설정 - Step 5](#step-5-storage-bucket-생성-1분) 참고**

---

#### Step 2: 관리자 페이지에서 업로드

1. http://localhost:3000/auth 로그인
2. http://localhost:3000/admin/portfolio 접속
3. **새 포트폴리오 추가** 클릭
4. 폼 작성:
   - 제목: "봄날의 웨딩"
   - 카테고리: "wedding" 선택
   - 설명: "아름다운 봄날의 야외 웨딩 스냅"
   - 이미지: 파일 선택 (JPG/PNG, 최대 10MB)
5. **저장** 클릭

**자동으로 처리되는 것:**
- ✅ Supabase Storage에 업로드
- ✅ DB에 URL 저장
- ✅ 포트폴리오 페이지에 즉시 표시

---

#### Step 3: 기존 Unsplash 데이터 삭제 (선택)

실제 사진을 충분히 올렸다면:

```sql
-- SQL Editor에서 실행
DELETE FROM portfolios
WHERE image_url LIKE '%unsplash.com%';
```

---

### 📊 이미지 최적화 팁

**업로드 전 확인:**
- ✅ 해상도: 1920x1080 이상
- ✅ 파일 크기: 2MB 이하 (압축 권장)
- ✅ 포맷: JPG (PNG는 용량 큼)

**압축 도구:**
- https://tinypng.com (무료)
- https://squoosh.app (Google 제공)

---

## 10. 관리자 보안 강화

### 🛡️ 현재 적용된 보안

프로젝트에 이미 포함된 보안 기능:

#### 1. 이메일 화이트리스트

특정 이메일만 관리자 접근 가능:

```typescript
// src/lib/admin-auth.ts
const ADMIN_EMAILS = [
  'admin@yoursite.com',
  'owner@yoursite.com',
];

export function isAdminEmail(email: string) {
  return ADMIN_EMAILS.includes(email.toLowerCase());
}
```

---

#### 2. 로그인 시도 제한 (Rate Limiting)

**5회 실패 시 15분 차단**

```typescript
// src/lib/rate-limiter.ts
const MAX_ATTEMPTS = 5;
const BLOCK_DURATION = 15 * 60 * 1000; // 15분

export function checkRateLimit(ip: string) {
  // IP 기반 로그인 시도 추적
}
```

---

#### 3. 세션 타임아웃

**15분 비활성 시 자동 로그아웃**

```typescript
// src/lib/session-manager.ts
const SESSION_TIMEOUT = 15 * 60 * 1000; // 15분

export function checkSession() {
  const lastActivity = localStorage.getItem('lastActivity');
  // 타임아웃 체크
}
```

---

#### 4. Row Level Security (RLS)

DB 레벨에서 권한 체크:

```sql
-- 관리자만 삭제 가능
CREATE POLICY "Admin can delete" ON contacts
FOR DELETE USING (
  (auth.jwt()->'user_metadata'->>'role') = 'admin'
);
```

---

### 🔐 추가 보안 (선택사항)

#### 관리자 URL 난독화

기본 `/admin` 대신 예측 불가능한 URL 사용:

**Step 1: 폴더명 변경**

```bash
# admin 폴더 이름 변경
mv src/app/admin src/app/secret-panel-x9k2m
```

**Step 2: 모든 링크 수정**

```tsx
// Before
<Link href="/admin">관리자</Link>

// After
<Link href="/secret-panel-x9k2m">관리자</Link>
```

---

# Part 4: 커스터마이징

## 11. 비즈니스 정보 변경

### 📞 가장 쉬운 커스터마이징!

**단 하나의 파일만 수정하면 전체 사이트가 변경됩니다!**

#### `src/config/business-info.ts` 열기

```typescript
export const businessInfo = {
  // 🏢 스튜디오 이름
  name: 'Moment Snap',  // ← 여기 수정!
  
  // 📧 이메일 (문의 수신)
  email: 'contact@momentsnap.com',  // ← 여기 수정!
  
  // 📞 전화번호
  phone: '+82-10-1234-5678',  // ← 여기 수정!
  
  // 📍 주소
  address: '서울특별시 강남구 테헤란로 123',  // ← 여기 수정!
  
  // 🌐 소셜 미디어
  social: {
    instagram: 'https://instagram.com/momentsnap',  // ← 여기 수정!
    facebook: 'https://facebook.com/momentsnap',    // ← 여기 수정!
    // YouTube 추가하고 싶다면:
    youtube: 'https://youtube.com/@momentsnap',
  },
  
  // ⏰ 영업시간
  hours: {
    weekday: '10:00 - 19:00',  // ← 여기 수정!
    weekend: '예약제',          // ← 여기 수정!
    holiday: '휴무',
  },
};
```

**수정 후 자동 반영되는 곳:**
- ✅ Footer (푸터)
- ✅ Contact 페이지
- ✅ Meta 태그 (SEO)
- ✅ 모든 소셜 링크

---

## 12. 색상 테마 변경

### 🎨 브랜드 색상 커스터마이징

#### `src/app/globals.css` 열기

```css
@theme {
  /* 📌 주요 색상 (Primary) */
  --color-primary-50: #fafaf9;   /* 아주 연한 색 */
  --color-primary-100: #f5f5f4;
  --color-primary-200: #e7e5e4;
  --color-primary-300: #d6d3d1;
  --color-primary-400: #a8a29e;
  --color-primary-500: #78716c;  /* 메인 색상 ← 여기 수정! */
  --color-primary-600: #57534e;
  --color-primary-700: #44403c;
  --color-primary-800: #292524;
  --color-primary-900: #1c1917;  /* 아주 진한 색 */
  
  /* ✨ 강조 색상 (Accent) */
  --color-accent: #d4a574;  /* 골드 ← 여기 수정! */
  
  /* 🌙 다크모드 (선택사항) */
  --color-dark-bg: #1c1917;
  --color-dark-text: #fafaf9;
}
```

---

### 🎯 색상 선택 팁

#### 추천 색상 조합 (포토그래퍼)

**1. 클래식 & 우아함**
```css
--color-primary-500: #78716c;  /* 그레이 */
--color-accent: #d4a574;       /* 골드 */
```

**2. 모던 & 심플**
```css
--color-primary-500: #374151;  /* 다크 그레이 */
--color-accent: #f59e0b;       /* 앰버 */
```

**3. 감성 & 따뜻함**
```css
--color-primary-500: #9f7a5c;  /* 베이지 브라운 */
--color-accent: #f4a261;       /* 코랄 */
```

**4. 트렌디 & 세련됨**
```css
--color-primary-500: #6b7280;  /* 그레이 */
--color-accent: #ec4899;       /* 핑크 */
```

---

### 🖼️ 적용 예시

색상을 변경하면 이런 곳이 자동으로 바뀝니다:

```tsx
// 버튼
<button className="bg-primary-500 hover:bg-primary-600">
  클릭
</button>

// 강조 텍스트
<h2 className="text-accent">
  특별한 순간을 담습니다
</h2>

// 카드
<div className="bg-primary-50 border-primary-200">
  콘텐츠
</div>
```

---

## 13. 다국어 관리

### 🌍 지원 언어

현재 3개 언어 지원:
- 🇰🇷 한국어 (Korean)
- 🇺🇸 영어 (English)
- 🇯🇵 일본어 (Japanese)

---

### 📝 번역 파일 수정

#### `src/locales/ko/common.json` (한국어)

```json
{
  "nav": {
    "home": "홈",
    "portfolio": "포트폴리오",
    "services": "서비스",
    "contact": "문의하기"
  },
  "hero": {
    "title": "소중한 순간을 영원히",
    "subtitle": "감성 스냅 전문 포토그래퍼",
    "cta": "문의하기"
  }
}
```

#### `src/locales/en/common.json` (영어)

```json
{
  "nav": {
    "home": "Home",
    "portfolio": "Portfolio",
    "services": "Services",
    "contact": "Contact"
  },
  "hero": {
    "title": "Capture Your Precious Moments",
    "subtitle": "Professional Snap Photographer",
    "cta": "Contact Us"
  }
}
```

---

### ➕ 새 언어 추가하기

#### 중국어 추가 예시

**Step 1: 폴더 생성**

```bash
mkdir src/locales/zh
```

**Step 2: 번역 파일 복사**

```bash
cp src/locales/ko/common.json src/locales/zh/common.json
```

**Step 3: 번역**

```json
// src/locales/zh/common.json
{
  "nav": {
    "home": "首页",
    "portfolio": "作品集",
    "services": "服务",
    "contact": "联系我们"
  }
}
```

**Step 4: 설정 추가**

```typescript
// src/i18n/config.ts
export const locales = ['ko', 'en', 'ja', 'zh'];  // ← 'zh' 추가

export const localeNames = {
  ko: '한국어',
  en: 'English',
  ja: '日本語',
  zh: '中文',  // ← 추가
};
```

---

# Part 5: 배포 및 운영

## 14. Vercel 배포

### 🚀 가장 쉬운 배포 방법!

**Vercel = Next.js 개발사가 만든 호스팅**
- ✅ 완전 무료 (취미 프로젝트)
- ✅ 자동 HTTPS
- ✅ CDN 포함 (전 세계 빠른 로딩)
- ✅ Git Push만 하면 자동 배포

---

### 🔧 배포 단계

#### Step 1: GitHub에 푸시 (5분)

```bash
# Git 초기화 (처음만)
git init
git add .
git commit -m "Initial commit"

# GitHub에 새 저장소 생성 후
git remote add origin https://github.com/your-username/photo-studio.git
git push -u origin main
```

---

#### Step 2: Vercel 연동 (3분)

1. https://vercel.com 접속
2. **Sign Up with GitHub** 클릭
3. **Import Project** 클릭
4. GitHub에서 `photo-studio` 저장소 선택

---

#### Step 3: 환경 변수 설정 (2분)

**Environment Variables** 섹션에서 추가:

```
NEXT_PUBLIC_SUPABASE_URL = https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY = your-anon-key-here
GMAIL_USER = your-email@gmail.com
GMAIL_APP_PASSWORD = your-16-digit-password
```

---

#### Step 4: 배포! (2분)

**Deploy** 버튼 클릭!

약 2분 후:
```
✅ Production: https://photo-studio-abc123.vercel.app
```

---

### 🔄 자동 배포 설정

**이후로는 Git Push만 하면 자동 배포!**

```bash
# 코드 수정 후
git add .
git commit -m "Update contact info"
git push

# Vercel이 자동으로 감지하고 배포!
```

---

## 15. 도메인 연결

### 🌐 나만의 도메인 사용하기

**기본 URL:**
```
https://photo-studio-abc123.vercel.app  (무료, 복잡함)
```

**커스텀 도메인:**
```
https://momentsnap.com  (연 12,000원, 전문적)
```

---

### 🔧 도메인 연결 단계

#### Step 1: 도메인 구매 (10분)

**추천 구매처:**
- **Cloudflare** (제일 저렴, 연 10,000원)
- **가비아** (한국어 지원, 연 15,000원)
- **Google Domains** (연 13,000원)

**도메인 예시:**
- `momentsnap.com`
- `김스냅.com` (한글 가능!)
- `moment-snap.com`

---

#### Step 2: Vercel에 도메인 추가 (5분)

1. Vercel 프로젝트 대시보드
2. **Settings** > **Domains**
3. 구매한 도메인 입력 (예: `momentsnap.com`)
4. **Add** 클릭

---

#### Step 3: DNS 설정 (10분)

Vercel이 제공하는 DNS 레코드를 도메인 구매처에 추가:

**Cloudflare 예시:**
```
Type: A
Name: @
Value: 76.76.21.21

Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

**10-30분 후 도메인 연결 완료!**

---

## 16. 비용 안내

### 💰 월간 비용 분석

#### 무료 플랜 (충분함!)

| 항목 | 비용 | 제한 |
|------|------|------|
| **Vercel** | 무료 | 100GB 대역폭/월 |
| **Supabase** | 무료 | 500MB DB, 1GB Storage |
| **Gmail API** | 무료 | 500통/일 |
| **도메인** | 연 12,000원 | - |
| **총계** | **월 1,000원** | 월 5,000 방문자까지 |

---

#### 유료 전환 시점

**월 방문자 5,000명 초과 시:**

| 항목 | 무료 | 유료 |
|------|------|------|
| **Vercel** | 무료 (100GB/월) | $20/월 (1TB/월) |
| **Supabase** | 무료 (500MB) | $25/월 (8GB) |
| **도메인** | - | 그대로 |
| **총계** | 월 1,000원 | **월 60,000원** |

---

#### 💡 비용 절감 팁

**1. 이미지 최적화**
- JPG 사용 (PNG 대신)
- 압축 (TinyPNG)
- WebP 포맷 (50% 용량 감소)

**2. Cloudflare 사용**
- 무료 CDN
- 무료 HTTPS
- 대역폭 절약

**3. 정기적인 DB 정리**
- 처리 완료한 문의 삭제
- 오래된 세션 정리

---

## 17. 사장님께 인수인계

### 📦 전달 자료

#### 1. 관리자 계정 정보

```
웹사이트: https://momentsnap.com
관리자 페이지: https://momentsnap.com/admin

로그인 정보:
- 이메일: admin@momentsnap.com
- 비밀번호: [안전한 비밀번호]

⚠️ 비밀번호는 절대 공유하지 마세요!
```

---

#### 2. 사용 가이드 (1페이지)

```markdown
# Moment Snap 웹사이트 사용 가이드

## 📱 페이지 구성
- 홈: momentsnap.com
- 포트폴리오: momentsnap.com/portfolio
- 서비스: momentsnap.com/services
- 문의: momentsnap.com/contact

## 🔑 관리자 기능
1. momentsnap.com/admin 접속
2. 로그인
3. 문의 내역 확인
4. 처리 완료한 문의 삭제

## 💰 월 비용
- 웹호스팅: 무료 (Vercel)
- 데이터베이스: 무료 (Supabase)
- 도메인: 연 12,000원
- 총 월 1,000원

## 📞 개발자 연락처
- 이름: [개발자명]
- 전화: [전화번호]
- 이메일: [이메일]
```

---

#### 3. 유지보수 계약서 (선택)

**베이직 플랜: 월 50,000원**
- 간단한 텍스트 수정 (월 3회)
- 사진 교체 (월 3장)
- 24시간 이내 응답
- 사이트 모니터링

**프리미엄 플랜: 월 100,000원**
- 베이직 플랜 포함
- 사진 무제한 교체
- 새 페이지 추가 (월 1개)
- SEO 최적화
- 월간 리포트

---

### 🎓 교육 세션 (30분)

#### 1부: 관리자 페이지 사용법 (15분)

**실습:**
1. 로그인하기
2. 문의 확인하기
3. 문의 삭제하기
4. 로그아웃하기

---

#### 2부: 간단한 수정 방법 (15분)

**실습:**
1. 전화번호 변경 요청 방법
2. 사진 교체 요청 방법
3. 가격 변경 요청 방법

---

### 📋 인수인계 체크리스트

```
□ 사이트 정상 작동 확인
□ 모든 페이지 둘러봄
□ 관리자 로그인 성공
□ 테스트 문의 제출
□ 문의 확인 및 삭제 테스트
□ 이메일 알림 수신 확인
□ 관리자 계정 정보 전달
□ 사용 가이드 전달
□ 30분 교육 완료
□ 긴급 연락처 교환
```

---

# Part 6: 문제 해결

## 18. 자주 묻는 질문 (FAQ)

### Q1. Unsplash 이미지가 안 보여요!

**원인:**
`next.config.ts`에 도메인이 설정되지 않음

**해결:**
```typescript
// next.config.ts
images: {
  remotePatterns: [
    {
      protocol: 'https',
      hostname: 'images.unsplash.com',  // ← 이 줄 확인!
    },
  ],
}
```

**서버 재시작 필수!**
```bash
# Ctrl+C로 중지
npm run dev
```

---

### Q2. 로그인이 안 돼요!

**원인 1: 계정이 없음**
```sql
-- SQL Editor에서 확인
SELECT * FROM auth.users;
```

없으면 [7. Supabase 설정 - Step 4](#step-4-관리자-계정-생성-2분) 참고

---

**원인 2: 관리자 권한 없음**
```sql
-- SQL Editor에서 실행
UPDATE auth.users
SET raw_user_meta_data = raw_user_meta_data || '{"role": "admin"}'::jsonb
WHERE email = 'your-admin@email.com';
```

---

**원인 3: Auto Confirm 안 됨**
```
Supabase Dashboard
> Authentication
> Users
> 해당 유저 클릭
> "Send confirmation email" 또는 수동 확인
```

---

### Q3. 문의 폼이 작동 안 해요!

**원인 1: 테이블 없음**
```sql
-- SQL Editor에서 확인
SELECT * FROM contacts;
```

에러 나면 `supabase-setup.sql` 다시 실행

---

**원인 2: RLS 정책 문제**
```sql
-- SQL Editor에서 실행
-- 모든 정책 확인
SELECT * FROM pg_policies WHERE tablename = 'contacts';
```

없으면 `supabase-setup.sql` 다시 실행

---

### Q4. 이메일 알림이 안 와요!

**원인 1: Gmail 설정 안 함**

`.env.local` 파일 확인:
```bash
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=abcdefghijklmnop
```

없으면 [8. Gmail 이메일 알림](#8-gmail-이메일-알림) 참고

---

**원인 2: 앱 비밀번호 오류**

공백 제거 확인:
```bash
# 잘못된 예
GMAIL_APP_PASSWORD=abcd efgh ijkl mnop

# 올바른 예
GMAIL_APP_PASSWORD=abcdefghijklmnop
```

---

**원인 3: 서버 재시작 안 함**
```bash
# Ctrl+C로 중지 후
npm run dev
```

---

### Q5. 포트폴리오가 안 보여요!

**원인: 데이터 없음**

SQL Editor에서 샘플 데이터 삽입:
```sql
-- INSERT_SAMPLE_PORTFOLIOS.sql 파일 실행
-- 또는 관리자 페이지에서 직접 업로드
```

---

### Q6. 배포 후 환경 변수 에러!

**원인: Vercel에 환경 변수 설정 안 함**

**해결:**
1. Vercel Dashboard
2. 프로젝트 선택
3. **Settings** > **Environment Variables**
4. `.env.local`의 모든 변수 추가
5. **Redeploy** 클릭

---

### Q7. 도메인 연결이 안 돼요!

**원인: DNS 전파 시간**

**해결:**
- 10-30분 대기 (보통 10분)
- 최대 48시간 소요 가능
- https://dnschecker.org 에서 확인

---

## 19. 트러블슈팅

### 🔥 에러별 해결 방법

#### Error: "Supabase URL is not defined"

**원인:** `.env.local` 파일 없음 또는 오타

**해결:**
```bash
# .env.local 파일이 있는지 확인
ls -la .env.local

# 없으면 생성
cp env.example .env.local

# 내용 확인
cat .env.local
```

---

#### Error: "Could not find table 'portfolios'"

**원인:** DB 테이블 생성 안 됨

**해결:**
```sql
-- SQL Editor에서 실행
-- CREATE_PORTFOLIOS_TABLE.sql 전체 내용 복사 & 실행
```

---

#### Error: "Row Level Security is enabled"

**원인:** RLS 정책 없음

**해결:**
```sql
-- SQL Editor에서 실행
-- supabase-setup.sql 전체 내용 재실행
```

---

#### Error: "Invalid login credentials"

**원인:** 비밀번호 오류 또는 계정 미확인

**해결:**
```
Supabase Dashboard
> Authentication
> Users
> 해당 유저 "Confirmed" 상태 확인
```

---

#### Error: "Network request failed"

**원인:** Supabase URL 오류 또는 인터넷 연결

**해결:**
```bash
# .env.local 확인
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co  # ← 올바른지 확인

# 서버 재시작
npm run dev
```

---

#### Error: "Rate limit exceeded"

**원인:** 로그인 5회 이상 실패

**해결:**
- 15분 대기
- 또는 브라우저 캐시 삭제
- 또는 시크릿 모드에서 시도

---

### 🛠️ 개발 서버 문제

#### 포트 충돌 (Port 3000 already in use)

**해결:**
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID [PID번호] /F

# Mac/Linux
lsof -i :3000
kill -9 [PID번호]

# 또는 다른 포트 사용
PORT=3001 npm run dev
```

---

#### Turbopack 에러

**해결:**
```bash
# .next 폴더 삭제
rm -rf .next

# node_modules 재설치
rm -rf node_modules
npm install

# 서버 재시작
npm run dev
```

---

### 📱 브라우저 문제

#### 이미지가 깨져 보여요

**해결:**
1. 하드 리프레시: `Ctrl + Shift + R`
2. 캐시 삭제
3. 시크릿 모드 테스트

---

#### CSS가 적용 안 돼요

**해결:**
```bash
# Tailwind 재빌드
npm run build

# 서버 재시작
npm run dev
```

---

### 🗄️ Supabase 문제

#### "Too many requests" 에러

**원인:** 무료 플랜 제한 초과

**해결:**
- 1시간 대기
- 또는 Pro 플랜 업그레이드 ($25/월)

---

#### Storage 업로드 실패

**원인:** 파일 크기 초과 또는 권한 없음

**해결:**
```sql
-- Storage 정책 확인
SELECT * FROM pg_policies WHERE tablename = 'objects';

-- 관리자 권한 확인
SELECT raw_user_meta_data FROM auth.users
WHERE email = 'your-admin@email.com';
```

---

# 🎉 완료!

## ✅ 이제 할 수 있는 것

- ✅ 프로젝트 완전히 이해
- ✅ 로컬 개발 환경 설정
- ✅ Supabase 데이터베이스 구축
- ✅ Gmail 이메일 알림 설정
- ✅ 포트폴리오 이미지 관리
- ✅ 비즈니스 정보 커스터마이징
- ✅ Vercel 배포
- ✅ 도메인 연결
- ✅ 문제 해결

---

## 📚 참고 자료

### 공식 문서
- **Next.js**: https://nextjs.org/docs
- **Tailwind CSS**: https://tailwindcss.com/docs
- **Supabase**: https://supabase.com/docs
- **Vercel**: https://vercel.com/docs

### 커뮤니티
- **Next.js Discord**: https://discord.gg/nextjs
- **Supabase Discord**: https://discord.supabase.com
- **Stack Overflow**: https://stackoverflow.com

---

## 💡 다음 단계

### 추가 기능 아이디어

**1. 블로그 기능**
- 촬영 팁 공유
- 고객 후기
- SEO 향상

**2. 온라인 예약**
- 캘린더 통합
- 카카오 알림톡
- 자동 일정 관리

**3. 결제 시스템**
- 패키지 온라인 결제
- 토스페이먼츠 연동
- 자동 영수증 발송

**4. 고객 갤러리**
- 고객별 비공개 갤러리
- 사진 다운로드
- 비밀번호 보호

**5. 리뷰 시스템**
- 고객 후기 수집
- 별점 평가
- 자동 SNS 공유

---

## 🙏 감사합니다!

이 가이드가 도움이 되셨기를 바랍니다!

**질문이나 문제가 있으시면:**
- GitHub Issues
- 이메일
- 카카오톡

**행복한 개발 되세요! 🚀**

---

**작성일**: 2025년 11월 3일  
**버전**: 2.0.0  
**작성자**: AI Assistant  
**업데이트**: 모든 기존 가이드 통합 완료

