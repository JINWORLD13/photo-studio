# Supabase 설정
# Supabase 대시보드 > Settings > API에서 복사하세요
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Gmail 설정 (문의 알림 이메일 발송용)
# Gmail 계정 설정 > 보안 > 앱 비밀번호에서 생성
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-digit-app-password

# 🔒 관리자 보안 설정 (선택사항)
# 관리자 이메일 화이트리스트 (쉼표로 구분)
# 이 이메일들만 관리자 페이지에 접근 가능
NEXT_PUBLIC_ADMIN_EMAILS=admin@yourdomain.com,owner@yourdomain.com

# 관리자 페이지 비밀 경로 (기본값: admin)
# 예측하기 어려운 경로로 설정하면 보안이 강화됩니다
NEXT_PUBLIC_ADMIN_PATH=my_secret_admin_2024

# 사용법:
# 1. 이 파일을 .env.local로 복사하세요
# 2. 위의 값들을 실제 API 키로 교체하세요
# 3. .env.local 파일은 Git에 업로드되지 않습니다 (보안)
# 4. 보안 설정은 선택사항이지만 강력히 권장합니다

